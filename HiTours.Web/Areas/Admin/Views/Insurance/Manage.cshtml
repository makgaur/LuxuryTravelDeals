@model AddInsuranceMasterViewModel;
@{
    this.ViewBag.Title = ViewData["Title"] = (Model.Id == 0 ? "Add Insurance" : "Edit Insurance");
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col-md-12">
        <div class="box no-top-border">
            <div class="box-header with-border">
                <h3 class="box-title">@this.ViewBag.Title</h3>
                <div class="pull-right top-buttons">
                    @this.Html.ButtonTop(new[] { new Button(Enums.ButtonType.ViewList, Url.Action("index","insurance" ,new { @area="Admin" })) })
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="box no-top-border">
                        @this.Html.Partial("_AddInsuranceMaster", Model);
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

